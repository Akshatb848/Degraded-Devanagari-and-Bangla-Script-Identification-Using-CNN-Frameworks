# Python 3.13 compatible — all packages have pre-built wheels (no source builds)

# ── Streamlit UI ──────────────────────────────────────────────────────────────
streamlit==1.43.2
pandas==2.2.3

# ── Image Processing ──────────────────────────────────────────────────────────
# numpy 2.x is required — 1.x has no Python 3.13 wheel (caused the distutils crash)
numpy==2.2.3
Pillow==11.1.0
# opencv 4.10.x is the first release with Python 3.13 wheels
opencv-python-headless==4.10.0.84

# ── OCR ───────────────────────────────────────────────────────────────────────
pytesseract==0.3.13

# ── LLM APIs ──────────────────────────────────────────────────────────────────
anthropic==0.49.0
openai==1.68.2

# ── Multi-Agent Orchestration ─────────────────────────────────────────────────
# langchain-core pulled automatically by langgraph — no separate pin needed
langgraph==0.3.18

# ── Config & Logging ──────────────────────────────────────────────────────────
pydantic==2.10.6
pydantic-settings==2.8.1
structlog==25.1.0
python-dotenv==1.0.1

# ── Build tool shim (distutils removed in Python 3.12+) ──────────────────────
setuptools==75.8.2
